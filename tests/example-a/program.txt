a: "x = 3" {
    invalidate_lease('L_x)
    goto b
}

b: "y = &'L_x x" {
    access_origin('x)
    clear_origin('y)
    introduce_lease('L_x, 'y)
    goto c
}

c: "x = 4" {
    // x is modified, invalidating lease
    invalidate_lease('L_x)
    goto d
}

d: "drop(y)" {
    access_origin('y)
    goto
}